# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test
runtime
conf/config.ini
# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.idea
# Dependency directories (remove the comment below to include it)
# vendor/
node_modules
config/locales/config.yaml
go.sum
ci
main
logs
static/imgs/product

# config
config/locales/config.yaml

# tools
/tools

# 压测运行时生成的 token，由 stressTest/setup_token.sh 重建
stressTest/config.json

# macOS
.DS_Store

# AI 指令文件，不入公开仓库（保留在本地）
CLAUDE.md