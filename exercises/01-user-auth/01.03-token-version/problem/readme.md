# 01.03 让旧 JWT 立即失效

完成 `Authorize`。一次请求必须依次通过：

1. token 签名有效；
2. token 尚未过期；
3. claims 中的用户与当前数据库用户一致；
4. token 版本号等于用户当前版本号；
5. 当前数据库角色满足接口要求。

`claims.Role` 是签发时的旧快照，不能拿它替代当前角色。改密码或客服强制下线时，数据库里的 `TokenVersion` 自增，旧 token 应立刻失败。
