# 07.02 参考实现说明

参考实现先做无副作用的参数校验，再把事件幂等判断、商品版本判断、商品写入和事件写入全部放进一个事务回调。

事件已经存在且商品 ID、版本一致时直接返回成功，不重复覆盖商品；否则同一事件 ID 的含义发生变化，返回冲突。新事件写入失败时，事务副本不会替换数据库原状态，因此商品和事件一起回滚。

验证命令：

```bash
go test -tags exercise ./exercises/07-product-search/07.02-index-outbox/solution
```
