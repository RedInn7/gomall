# 00.01 谁说了算

客户端请求里同时带了 `UserID`、价格和卖家，但这些字段都可能被篡改或已经过期。请完成 `BuildOrder`：

- 订单归属取鉴权上下文中的 `authUserID`；
- 价格与卖家取服务端查询到的 `Product`；
- 请求只提供商品 ID、数量和地址 ID；
- 数量不大于 0、商品 ID 不匹配或地址不属于当前用户时返回对应错误。

公开测试覆盖正常下单和常见篡改。评分测试还会检查零值、负数以及不同的伪造字段。
