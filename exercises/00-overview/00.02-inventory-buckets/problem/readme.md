# 00.02 两桶库存

实现一个内存版 `available / reserved` 库存模型：

- `Reserve(qty)`：可售转为预留；
- `Commit(qty)`：支付成功，预留转为已售；
- `Release(qty)`：取消或超时，预留退回可售。

任何失败都不能改动库存；数量必须为正；预扣不能超过可售，提交或释放不能超过预留。每次操作后还要守住“库存不能为负”和“总量守恒”。
