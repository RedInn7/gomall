# 08.01 参考思路

先忽略失败链路，再用 map 对每一路按商品 ID 去重。关键词保留最高分，向量保留最小距离。去重后的值用于计算各自的最小值和最大值；极差为零时整路记为 `1`，避免除零。

随后把两路归一化结果写入同一个商品 map。两路都有候选时使用固定的 `0.5 / 0.5` 权重；降级为单路时使用健康链路的完整得分。最后复制到切片中，用最终分数降序和商品 ID 升序排序，再截取 TopK。整个过程不会改写输入切片，时间复杂度主要是 `O(n log n)`，其中排序前的去重和归一化都是线性的。
